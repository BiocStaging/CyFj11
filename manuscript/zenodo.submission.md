# Zenodo Submission — Companion Repository for CyFj11 Appendix S1/S2

This file lists the files expected in the Zenodo archive referenced by
`manuscript/supl.1.md` (DOI placeholder: `10.5281/zenodo.20933199` — **confirm/replace
before submission**). Paths are relative to `flowjo_export_tests/` in the CyFj11
git repository unless noted otherwise.

Scope: everything needed to regenerate and independently check the 14 validation
test scenarios described in Appendix S1, without requiring the full CyFj11
development git history.

## 1. Test-generation scripts

- `generate_all_tests.R` — authoritative generator; regenerates all 14 scenarios.
- `README.txt` — usage notes for the archive (add/update to describe archive layout).

Excluded (superseded/dev-only, do not archive): `generate_all_tests.v2.R`,
`update_paths.sh`, `history.txt`.

## 2. Development notebook

- `roundtrip_experiment.Rmd` — original exploration and visual checks that
  informed the generator (referenced in Appendix S1 as not required for
  reproducing results, but included for provenance).

## 3. Per-test synthetic data and exported workspaces (tests 01–13)

For each `testNN` in `01`–`13`:

- `testNN/sampleNN.fcs` — synthetic input FCS file.
- `testNN/test_sample_0NN.fcs` — as generated by `create_test_fcs()` (raw output
  before copy/rename into `sampleNN.fcs`; confirm whether both are needed or
  one is redundant before archiving).
- `testNN/testNN_export.wsp` — FlowJo v10 workspace produced by
  `export_flowjo10_workspace()`.
- `testNN/testNN_export.llm.wsp` — comparison copy saved from FlowJo v10 (present
  for tests 01–13; **confirm test05–13's `testNN.flowjo` files, see note below**).

Note: tests 05, 06, 07, 08, 11, 12, 13 currently also contain a `testNN.flowjo`
file (FlowJo v11 project file) not mentioned in Appendix S1 A.5 — clarify
whether this is expected/required for the archive or a leftover working file.

## 4. Test 14 — real-world FlowSOM dataset

- `test14/68983.fcs` — public FlowSOM example FCS (copied from the FlowSOM
  Bioconductor package's `extdata/`; confirm licensing/attribution is fine to
  redistribute, or instead document the exact FlowSOM package version to
  regenerate it rather than archiving the raw file).
- `test14/sample14.fcs`
- `test14/test14_flowsom.wsp` — original FlowSOM `gating.wsp` import basis.
- `test14/test14_export.wsp` — CyFj11-exported FlowJo v10 workspace.
- `test14/test14_export.flowjo` — FlowJo v11 project file.
- `test14/export_test14.R` — script used to run test 14 (currently uncommitted
  in git working tree — verify it matches what's described in Appendix S1
  before archiving).
- `test14/extract_nk1_populations.R` — supporting analysis script for the
  NK1/1 → NK1_1 rewrite finding described in Appendix S1 (table row 14).
- `test14/FJ11.singlets2.png`, `test14/R_NK1_1_gate.png` — supporting figures
  (confirm whether these belong in the archive vs. the manuscript figures).

Excluded (superseded working copies, do not archive): `test14.old/`,
`test14.old2/`, `sample14.org.fcs`, `sample14.org.fcs.comp.bak`.

## 5. Validation results referenced in Appendix S2

- `cyfj11_validation_summary.csv`
- `cyfj11_validation_detail.csv`
- `cyfj11_validation_report.txt`
- `validation_counts.csv` (and/or `.tsv` — pick one canonical format; having
  both `validation_counts.csv`, `validation_counts.tsv`, and
  `validation_counts.numbers` in the archive is redundant)
- `validation_transforms.tsv`
- `wsp_comparison_report.md`
- `wsp_semantic_comparison_report.md`
- `WSP_LLM_DIFFERENCES_SUMMARY.md`

## 6. Explicitly excluded from the archive

- `.DS_Store`
- `test14.old/`, `test14.old2/` (superseded experiment directories)
- `sample14.org.fcs`, `sample14.org.fcs.comp.bak`
- `generate_all_tests.v2.R`, `update_paths.sh`, `history.txt`
- `validation_counts.numbers` (proprietary format; keep `.csv`/`.tsv` only)
- `figure3_singlets2_panels.R` (manuscript figure-generation, not part of the
  validation pipeline — confirm and move to manuscript materials if so)

## Open items before submission

1. Confirm the Zenodo DOI in `supl.1.md` — it does not currently appear
   anywhere else in the repository and could not be verified.
2. Confirm which CyFj11 package version (`DESCRIPTION` currently at 0.99.95;
   `supl.1.md` cites 0.99.9) the archived outputs correspond to, and re-run
   `generate_all_tests.R` if the archive should reflect the current version.
3. Resolve the `testNN.flowjo` and `test_sample_0NN.fcs` redundancy questions
   above so the archive contains exactly one canonical copy of each artifact.
4. Confirm FlowSOM's `68983.fcs` may be redistributed, or replace with
   regeneration instructions.
